// Playwright configuration removed — Playwright E2E support has been disabled.
// If you need to re-enable E2E later, restore `playwright.config.mjs` from
// your VCS history or recreate it with the desired settings.
